## Description

The `_session.py` file provides session and logging utilities for the `cache_manager` package. It sets up a dedicated session context and logger, leveraging utilities from the `pypath_common` library. This module ensures that all cache operations are tracked within a consistent session and that logging is centralized, making it easier to monitor, debug, and audit cache-related activities throughout the package.

---

::: cache_manager._session
